#ifndef EXAR_MEMORY_FUNCTIONS_HPP
#define EXAR_MEMORY_FUNCTIONS_HPP

// #include "Engine/Runtime/RHI/Exar/ExarMemoryRequirements.hpp"
// #include "Engine/Runtime/RHI/Exar/IExarDevice.hpp"
// #include "Engine/Runtime/RHI/Exar/IExarBuffer.hpp"

namespace UnasciiEngine::RHI::EXAR
{
	//bool GetResourceMemoryRequirements(const IExarDevice* pDevice, const IExarBuffer* pBuffer, ExarMemoryRequirement*& pMemRequirement) noexcept
	//{
	//	if (!pDevice || !pBuffer) return false;

	//	size_t lSize = pBuffer->getDesc().size;
	//	size_t lAlign = 32;

	//	if (pDevice->)

	//	return true;
	//}
}

#endif // !EXAR_MEMORY_FUNCTIONS_HPP
